{
 "name": "02-tools-and-context",
 "version": "1.0.0",
 "description": "Contains two modules 'greet' and 'arithmetic' in lib. There are matching tests in __test__. These ensure that parameters are appropriate, or null if not.",
 "main": "index.js",
 "directories": {
   "lib": "lib"
 },
 "scripts": {
   "lint": "eslint .",
   "test": "jest -i --coverage"
 },
 "author": "",
 "license": "ISC",
 "devDependencies": {
   "jest": "^21.0.2"
 }
}
